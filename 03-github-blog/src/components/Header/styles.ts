import { styled } from "styled-components";
import cover from "../../assets/cover.svg";

export const HeaderBackground = styled.header`
  background: url(${cover}) no-repeat top center;
  padding: 10%;
  background-size: 100%;
`;
